	  <%= f.label :is_researcher, "Researcher", class: 'checkbox-inline' do %>
	  <%= f.check_box :is_researcher %> Researcher
	  <% end %>
	  <%= f.label :is_teacher, "Teacher", class: 'checkbox-inline' do %>
	  <%= f.check_box :is_teacher %> Teacher
	  <% end %>
	  <%= f.label :is_student, "Student", class: 'checkbox-inline' do %>
	  <%= f.check_box :is_student %> Student
	  <% end %>
	  <%= f.label :is_comm_member, "Community member", class: 'checkbox-inline' do %>
	  <%= f.check_box :is_comm_member %> Community member
	  <% end %>
