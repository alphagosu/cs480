<% provide(:title, 'All users') %>
<h1>Listing users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
  <li>
    <%= image_tag user.avatar.url(:thumb) %>
    <%= link_to user.first_name, user %>
  </li>
  <% end %>
</ul>
<%= will_paginate %>
